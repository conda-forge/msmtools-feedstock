{% set name = "msmtools" %}
{% set version = "1.2.2" %}
{% set sha256 = "d184266b5a81c04730f7fcfac09bbdb9936f78711262ddbe476d566c14e3f589" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - setuptools
    - cython >=0.24
    - numpy 1.8.*  # [not (win and (py35 or py36))]
    - numpy 1.9.*  # [win and py35]
    - numpy 1.11.*  # [win and py36]

  run:
    - python
    - numpy >=1.8  # [not (win and (py35 or py36))]
    - numpy >=1.9  # [win and py35]
    - numpy >=1.11  # [win and py36]
    - scipy
    - six
    - decorator

test:
  imports:
    - msmtools 

about:
  home: http://github.com/markovmodel/msmtools
  license: LGPL-3 
  license_file: LICENSE.txt
  summary: 'Tools for estimating and analyzing Markov state models'
  description: |
    'msmtools is an open source collection of algorithms for the estimation
     and analysis of discrete state space Markov chains via Markov state models (MSM)'

extra:
  recipe-maintainers:
    - marscher 
